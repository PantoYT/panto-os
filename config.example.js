// Copy this file to config.js and fill in your values
const CONFIG = {
  OWM_KEY: '',           // openweathermap.org API key
  TAILSCALE_KEY: '',     // Tailscale API key (tskey-api-...)
  TAILSCALE_TAILNET: '', // Tailscale tailnet name
  OLLAMA: '',            // Ollama URL, e.g. http://localhost:11434
  SYSAPI: '',            // sysapi URL, e.g. http://localhost:9001
}
